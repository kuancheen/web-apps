name: Update Project Data

on:
  schedule:
    - cron: '0 0 * * *' # Run daily at midnight
  workflow_dispatch: # Allow manual trigger

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch Star List Data
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Query to get all lists and their items
          QUERY='query($login: String!) {
            user(login: $login) {
              lists(first: 20) {
                nodes {
                  name
                  items(first: 100) {
                    nodes {
                      ... on Repository {
                        name
                        owner { login }
                        description
                        homepageUrl
                        url
                        stargazerCount
                        primaryLanguage { name color }
                      }
                    }
                  }
                }
              }
            }
          }'
          
          # Fetch the data using gh cli
          DATA=$(gh api graphql -f query="$QUERY" -F login='kuancheen')
          
          # Get current timestamp in ISO 8601 format
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          # Filter the specific list named "web-apps" and wrap with metadata
          echo "$DATA" | jq --arg timestamp "$TIMESTAMP" '{
            lastUpdated: $timestamp,
            repositories: [.data.user.lists.nodes[] | select(.name | contains("web-apps")) .items.nodes[]]
          }' > data.json

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update project data from GitHub Stars" && git push)
